= render 'layouts/header'
#wrapper
  = render 'layouts/sidebar'
  #content-wrapper
    .container-fluid
      / Breadcrumbs
      %ol.breadcrumb
        %li.breadcrumb-item
          = link_to 'Ordenes', orders_path
        %li.breadcrumb-item.active Editar Orden
      = render 'form'

= render 'layouts/footer'
